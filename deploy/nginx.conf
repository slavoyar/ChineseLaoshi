server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location /api/ {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass_request_headers on;
    }

    # Discovery Link headers only for site documents (not /api/)
    location = / {
        add_header Link "</sitemap.xml>; rel=\"sitemap\", </llms.txt>; rel=\"describedby\"; type=\"text/plain\", </about.html>; rel=\"about\"" always;
        try_files /index.html =404;
    }

    location = /about.html {
        add_header Link "</sitemap.xml>; rel=\"sitemap\", </llms.txt>; rel=\"describedby\"; type=\"text/plain\"" always;
    }

    location = /llms.txt {
        add_header Link "</sitemap.xml>; rel=\"sitemap\", </about.html>; rel=\"about\"" always;
        default_type text/plain;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
